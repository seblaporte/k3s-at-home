---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kitchenvault-backend
spec:
  values:
    controllers:
      main:
        containers:
          main:
            image:
              tag: "2026.8.0"
